# 18_SAMPLE_OUTPUTS_USAGE

> `records/examples/` 里的文件不是你的真实阅读结论，而是**示范写法**。

你应该这样用它们：

1. 看它们的组织方式
2. 看一份 `SUMMARY.md` 该怎么写
3. 看 `FUNCTION_NOTE` 该怎么落字段
4. 看 Round2 的路径总结应该写到什么粒度
5. 然后在自己的 `records/<ts>-roundX-*` 目录下按相同方式重新写

## 不建议做的事

- 不要把示范内容原封不动当成最终结论
- 不要把样例当作对你本地内核源码的精确解析结果
- 不要跳过自己的阅读和校对

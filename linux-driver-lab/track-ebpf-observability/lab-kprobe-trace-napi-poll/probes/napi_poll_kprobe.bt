kprobe:napi_poll
{
    @napi_poll_calls = count();
    @napi_poll_by_cpu[cpu] = count();
    @napi_poll_by_comm[comm] = count();
}

interval:s:1
{
    print(@napi_poll_calls);
    print(@napi_poll_by_cpu);
    print(@napi_poll_by_comm);
}

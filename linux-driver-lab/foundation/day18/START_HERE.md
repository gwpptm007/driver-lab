# Day18 START_HERE

如果你刚打开 `day18/`，建议这样看：

1. 先看 `README.md`
2. 再看 `docs/01_day18_requirement_analysis.md`
3. 执行 `python3 export_category_view.py`
4. 再跑 `PROFILE=classified BUILD_KERNEL=no ./apply_config.sh`
5. 最后再决定要不要直接跑 `./run_compare_profiles.sh`

第一次上手最短路径：

```bash
cd ~/workspace/driver-lab/linux-driver-lab/day18
python3 export_category_view.py
PROFILE=classified BUILD_KERNEL=no ./apply_config.sh
```

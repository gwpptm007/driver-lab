# 复制为 local.<yourname>.env 后按本机真实路径填写，再 source 使用。
# 如果 day27 已经编过 arm64 静态 lspci，可以直接把 GUEST_LSPCI_BIN 指向 day27 的产物，
# 这样 day29 就可以先跳过 make build-lspci。
export RUN_ID=day29-local-001
export KERNEL_SRC_ROOT=/path/to/driver-lab/kernel-src/linux-5.15.10/src
export KDIR=/path/to/driver-lab/kernel-src/linux-5.15.10/build/arm64
export KERNEL_IMAGE=/path/to/driver-lab/kernel-src/linux-5.15.10/build/arm64/arch/arm64/boot/Image
export KERNEL_CONFIG_PATH=/path/to/driver-lab/kernel-src/linux-5.15.10/build/arm64/.config
export BUSYBOX_BIN=/path/to/driver-lab/kernel-src/busybox-1.36.1/build/arm64/busybox
export PCIUTILS_SRC_DIR=/path/to/driver-lab/linux-driver-lab/day29/third_party/pciutils
export GUEST_LSPCI_BIN=/path/to/driver-lab/linux-driver-lab/day27/third_party/pciutils/lspci
export ARCH=arm64
export CROSS_COMPILE=aarch64-linux-gnu-
export DAY29_VERIFY_LEN=256
export DAY29_VERIFY_SEED=0x41

export QEMU_TIMEOUT_SEC=120

# arm64 virt 下推荐配套使用：QEMU EDU 设备放宽到 32-bit，驱动也按 32-bit 申请 DMA mask
export EDU_DMA_MASK_BITS=32
export EDU_QEMU_DMA_MASK=0xffffffff

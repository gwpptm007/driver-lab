# Day16 CONFIG_CONFIRMATION_03

## 1. round1 候选项 `.config` 实际确认结果

### 执行位置

宿主机：

```bash
cd ~/workspace/driver-lab/kernel-src/linux-5.15.10
```

### 实际 grep 结果

```text
CONFIG_BLK_DEV_LOOP=y
CONFIG_THUNDER_NIC_PF=y
CONFIG_THUNDER_NIC_BGX=y
CONFIG_HNS3=y
CONFIG_HNS3_ENET=y
CONFIG_E1000=y
CONFIG_E1000E=y
CONFIG_IGB=y
CONFIG_IGBVF=y
CONFIG_SKY2=y
CONFIG_I2C_CHARDEV=y
CONFIG_SOUND=y
CONFIG_SND=y
CONFIG_HID=y
CONFIG_HID_GENERIC=y
CONFIG_USB_HID=y
CONFIG_USB_SUPPORT=y
CONFIG_USB=y
CONFIG_USB_EHCI_HCD=y
CONFIG_USB_EHCI_HCD_PLATFORM=y
CONFIG_USB_OHCI_HCD=y
CONFIG_USB_OHCI_HCD_PLATFORM=y
CONFIG_USB_STORAGE=y
CONFIG_MMC=y
CONFIG_MMC_SDHCI=y
CONFIG_MMC_SDHCI_PLTFM=y
CONFIG_LEDS_TRIGGER_CPU=y
```

### 结论

- round1 候选项几乎全是 `=y`
- 确认了 round1 确实有可裁空间
- 其中网络驱动、声音、部分 USB 最适合第一轮先下手

---

## 2. round2 候选项 `.config` 实际确认结果

### DRM / HDMI / 显示桥

```text
CONFIG_DRM=m
CONFIG_DRM_KMS_HELPER=m
CONFIG_DRM_FBDEV_EMULATION=y
CONFIG_DRM_NOUVEAU=m
CONFIG_DRM_EXYNOS=m
CONFIG_DRM_ROCKCHIP=m
CONFIG_DRM_RCAR_DW_HDMI=m
CONFIG_DRM_MSM=m
CONFIG_DRM_BRIDGE=y
CONFIG_DRM_LONTIUM_LT9611=m
CONFIG_DRM_LONTIUM_LT9611UXC=m
CONFIG_DRM_SII902X=m
CONFIG_DRM_I2C_ADV7511=m
CONFIG_DRM_DW_HDMI=m
CONFIG_DRM_HISI_HIBMC=m
CONFIG_DRM_MEDIATEK=m
CONFIG_DRM_MESON=m
```

### SoundWire

```text
CONFIG_SOUNDWIRE=m
```

### I2C helper

```text
CONFIG_I2C_ALGOBIT=m
CONFIG_I2C_GPIO=m
```

### USB 剩余大块

```text
CONFIG_USB_SUPPORT=y
CONFIG_USB=y
CONFIG_USB_CHIPIDEA=y
CONFIG_USB_CHIPIDEA_TEGRA=y
```

### 结论

- round2 候选项大部分为 `=m`
- `USB_CHIPIDEA*` 为 `=y`，值得重点关注
- `DRM` 顶层与 `DRM_BRIDGE` 仍在，说明显示栈还没彻底退出

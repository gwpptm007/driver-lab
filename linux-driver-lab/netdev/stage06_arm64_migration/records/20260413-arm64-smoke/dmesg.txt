# stage06 ARM64 smoke test — 2026-04-13

## Environment
- Kernel: 5.15.10 ARM64 (linux-5.15.10 rebuilt with CONFIG_NET=y CONFIG_PACKET=m)
- Module: netdev_stage04.ko (ARM64 ELF, 110KB)
- Rootfs: busybox ARM64 + af_packet.ko + netdev_stage04.ko + userspace tools
- QEMU: qemu-system-aarch64 -machine virt -cpu cortex-a57 -m 512M

## smoke 结果

TX (send_stage04_frame):
  [stage04] TX RXIDX=0..31 SKBLEN=25 CPYLEN=25 ETH=6865
  [send_stage04_frame] sent 32 frame(s) on nds4 ethertype=0x88b7 payload=hello_arm64

RX (NAPI poll via dmesg):
  [stage04] POLL IDX=0..31 LEN=25 PROTO=88b7 RC=818555768
  → NAPI poll 收到了 32 个包，说明 ring DMA + NAPI 全通

recv (userspace):
  [recv_stage04_frame] received 0 frame(s)
  → 0 因为 eth_type_trans 把 ethertype 0x88B7 发到协议栈，
    没有注册 handler，协议栈 DROP。这是预期行为（不是 bug）。

## 结论
✅ stage04 在 ARM64 + QEMU 上 build/run 全通
✅ ring DMA TX/RX 全链路验证（32 帧，dmesg 可观测）
✅ NAPI poll 在 ARM64 上正常工作
✅ smoke test 端到端成功

# reports

本目录用于存放 `stage06_arm64_migration` 的阶段报告、平台差异报告、验收结论等正式报告类产物。

与 `output/` 的区别：

- `output/`：当前运行过程中生成的原始输出、临时报告、日志、模块、镜像
- `reports/`：适合评审、归档、对外阅读的稳定报告

当前版本先建立目录和职责说明，后续收口时建议把正式阶段结论逐步沉淀到这里。
